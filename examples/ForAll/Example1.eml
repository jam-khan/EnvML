-- Example 1: Contextual Polymorphism with first-class modules

type ENV = forall t. sig val id : t -> t;; end;;

module let env : ENV =
  functor (t : Type) -> 
      struct
        val id : t -> t = fun x -> x;;
      end;;

--let int_result : int    =
--  box [x = env @int] in x.id(1);;
--let bool_result : bool  =
--  box [x = env @bool] in x.id(true);;
