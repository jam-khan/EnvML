let fact : int -> int -> int =
	fun (p : int) -> fix f. fun (x : int) -> 
    (if x == 1 then 1 else (x * f(x - 1)) + p);

let placeholder : int = 0; -- test interaction with bindings outside fix
let result : int = fact(placeholder)(4);
