type NatList = mu x. (Nil : int | Cons : x);

-- Explicit fold at introduction
let a = fold [NatList] Nil(2);

-- Explicit unfold at elimination
let res =
	case unfold a of
		<Nil = x> => x
	| <Cons = x> => 0;
